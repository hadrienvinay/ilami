
{% extends "base.html.twig" %}

{% block title %}Convertisseur 🕹️{% endblock %}

{% block body %}
    <div class="page-content">
        <div class="panel-content">
            <h1 class="text-center">Le P'tit Convertisseur 🤓</h1>
            <br>
            <div class="row">
                <div class="col-md-4">
                </div>
                <div class="col-md-4">
                    {{ form_start(form, {'method': 'post', 'action': path('convertor'), 'attr': {'class': 'text-center'}}) }}
                    {{ form_row(form.url, {'attr': {'class': 'form-group'}}) }}
                    <div class="form-group text-center">
                        <input class=" btn btn-info btn-md login-button" type="submit" value="Télécharger" />
                    </div>
                    {{ form_rest(form) }}
                    {{ form_end(form) }}
                </div>
                <div class="col-md-4">
                </div>
            </div>
        </div>
    </div
{% endblock %}
