{% extends "base.html.twig" %}

{% block title %}Horaires{% endblock %}

    {% block body %}
        <br>
        <div class="container" style="padding: 3%;margin-top: 3%;margin-bottom: 3%;border-radius: 0.5rem;background: #fff;">
            <h1 class="text-center">Les P'tits Transports 🚌</h1>
            <br>
            <div class="my-3 p-3 bg-white rounded box-shadow">
                <h6 class="border-bottom border-gray pb-2 mb-0">Métro 🚃</h6>
                <i> Métro 10 :</i>
                {% for time in metro1 %}
                    {% if loop.index == 1 or loop.index == 5 %}
                        <div>
                        Direction : {{ time.destination }} :
                    {% endif %}
                        {{ time.message }} /
                    {% if loop.index == 4 %}
                        </div>
                    {% endif %}
                {% endfor %}
                </div>
                <h6 class="border-bottom border-gray pb-2 mb-0">Bus 🚍</h6>
                <i> BUS 52 :</i>
                {% for time in bus2 %}
                    {% if loop.index == 1 or loop.index == 3 %}
                        <div>
                        Direction : {{ time.destination }} :
                    {% endif %}
                    {{ time.message }} /
                    {% if loop.index == 2 %}
                        </div>
                    {% endif %}
                {% endfor %}
                </div>

                <i> BUS 72 :</i>
                {% for time in bus1 %}
                    {% if loop.index == 1 or loop.index == 3 %}
                        <div>
                        Direction : {{ time.destination }} :
                    {% endif %}
                    {{ time.message }} /
                    {% if loop.index == 2 %}
                        </div>
                    {% endif %}
                {% endfor %}
                </div>
            </div>

        </div>

    {% endblock %}
