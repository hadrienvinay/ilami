{% extends "base.html.twig" %}

{% block title %}Evènement - Création {% endblock %}

{% block body %}
<br>
    <div class="container" style="padding: 3%;margin-top: 3%;margin-bottom: 3%;border-radius: 0.5rem;background: #fff;">
        <h1 class="text-center">Un p'tit nouvel évent</h1>
        <br>
        {{ form_start(form, {'method': 'post', 'action': path('add_event'), 'attr': {'class': ''}}) }}
        {{ form_widget(form, {'attr': {'class': 'form-group'}}) }}
        <div class="form-group text-center">
            <input class=" btn btn-primary btn-md login-button" type="submit" value="Créer" />
        </div>
        {{ form_rest(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}

