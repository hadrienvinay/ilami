{% extends "base.html.twig" %}

{% block title %}Profil - New Pic 🛠️{% endblock %}

{% block body %}
    <div class="page-content">
        <div class="panel-content">
            <h1 class="text-center">Une p'tite modification d'photo d'profil 🙉</h1>
            <br>
            {{ form_start(form, {'method': 'post', 'action': path('modify_profile_pic',{'id': user.id}), 'attr': {'class': ''}}) }}
            {{ form_widget(form, {'attr': {'class': 'form-group'}}) }}
            <div class="form-group text-center">
                <input class=" btn btn-primary btn-md login-button" type="submit" value="Modifier" />
            </div>
            {{ form_rest(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}

